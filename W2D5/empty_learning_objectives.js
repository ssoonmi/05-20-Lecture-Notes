// - Given a folder structure diagram, a list of 'cd (path)' commands and target files, 
  // match the paths to the target files.

// - Be able to create folders, files, and .zips on your machine

// - Know the difference between Primitive and Reference variable types in JS

// - Identify when to use . vs [] when accessing values of an object. Practice using both notations. 

// - Use the obj[key] !== undefined pattern to check if a given variable that contains a key exists in an object

// - Know these methods: Object.keys and Object.values 

// - Iterate through an object using a `for in` loop

// - Define a function that utilizes ...rest syntax to accept an arbitrary number of arguments

// - Use ...spread syntax for Object literals and Array literals

// - Destructure an array to reference specific elements & destructure an object to reference specific values

// - Write a function that accepts a string as an argument and returns an object representing the 
  // count of each character in the string

// - Explain the difference between const, let, and var declarations

// - Predict the evaluation of code that utilizes function scope, block scope, and scope chaining

// - Define an arrow function

// - Implement a closure and explain how the closure effects scope

// - Define a method that references this on an object literal

// - Utilize the built in Function#bind on a callback to maintain the context of this

// - Given a code snippet, identify what 'this' refers to

// - Given multiple plausible reasons, identify why functions are called "First Class Objects" in JavaScript.

// - Given a code snippet containing an anonymous callback, a named callback, and multiple console.logs, 
  // predict what will be printed

// - Write a function that takes in a value and two callbacks. The function should return the result of the 
  // callback that is greater.

// - Write a function, myMap, that takes in an array and a callback as arguments. 
  // The function should mimic the behavior of Array#map.

// - Write a function, myFilter, that takes in an array and a callback as arguments. 
  // The function should mimic the behavior of Array#filter.

// - Write a function, myEvery, that takes in an array and a callback as arguments. 
  // The function should mimic the behavior of Array#every.